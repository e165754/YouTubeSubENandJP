# 変更履歴

## 2026-04-06（追記）

- 変更内容: 視聴ページの字幕フォントサイズを `src/app/globals.css` の CSS 変数（`--watch-subtitle-overlay-*` / `--watch-subtitle-script-*`）でまとめて調整できるようにした。既定は従来よりやや大きめ（オーバーレイ英語 1.3125rem 等）。`SubtitleOverlayPanel` / `TranscriptScriptPane` が参照。
- 変更理由: ユーザーが字幕の大きさを変えやすくするため。
- 影響範囲: `src/app/globals.css`, `SubtitleOverlayPanel.tsx`, `TranscriptScriptPane.tsx`, `docs/仕様書.md`, `docs/変更履歴.md`。

- 変更内容: IFrame の `getCurrentTime` と timedtext のタイミング差の補正として、`findActiveBilingualCueIndex` で行判定に **実効時刻 = 再生時刻 − ラグ** を使う。調整は `NEXT_PUBLIC_SUBTITLE_PLAYBACK_SYNC_LAG_SECONDS`（-5〜5）。**既定ラグを -0.35 秒**に変更（負なので実効時刻が進み、字幕切り替えがやや早まる）。
- 変更理由: ユーザー報告のタイミングずれ対策。あわせて既定ラグを **-0.35** に変更する依頼。
- 影響範囲: `src/lib/watch/findActiveBilingualCueIndex.ts`, `.env.example`, `docs/仕様書.md`, `docs/設計_MVP_v1.md`, `docs/変更履歴.md`。

- 変更内容: 直前に削除していた**日本語字幕の UI 表示**（オーバーレイ・スクリプトの日本語行、ヘッダーの表示モード／学習用弱表示）を**元に戻した**。
- 変更理由: 利用方針の見直し（ユーザー依頼）。
- 影響範囲: `SubtitleOverlayPanel.tsx`, `TranscriptScriptPane.tsx`, `YouTubeLearningWatchPageContent.tsx`, `docs/仕様書.md`, `docs/設計_MVP_v1.md`, `docs/変更履歴.md`。

- 変更内容: YouTube から**日本語が 1 行も取れない**動画では、マージ後の全行がローカル NLLB の対象になり逐次推論で `/api/captions` が極端に遅くなったりタイムアウトし、**英語字幕もクライアントに届かない**ことがあった。パース後の `japaneseCueList.length === 0` のときは**既定で NLLB をスキップ**し、英語のみ（`missingJapanese: true` のまま）を返す。全文をローカル翻訳したい場合のみ `ENABLE_LOCAL_NLLB_FOR_ENGLISH_ONLY_TRANSCRIPTS=1`。
- 変更理由: ユーザー報告「日本語字幕がない動画で英語も出ない」の実態が、全文 NLLB による応答失敗と整合するため。
- 影響範囲: `src/app/api/captions/route.ts`, `.env.example`, `docs/仕様書.md`, `docs/設計_MVP_v1.md`, `docs/ローカル字幕翻訳_NLLB.md`, `docs/デプロイ手順.md`, `docs/変更履歴.md`。

- 変更内容: macOS で ONNX ネイティブが **Node の標準ストリームを経由せず**ログを出すため、JS の `write` ラップでは消えない。`npm run dev` / `dev:turbo` を **`scripts/next-dev-filter-onnx-clean-unused-initializer-lines-on-stderr.sh`** 経由にし、`next` プロセスの **stderr をシェルで行フィルタ**してから端末（fd 3 退避）へ渡す。
- 変更理由: ユーザー環境で instrumentation / JS フィルタだけではログが変わらなかったため。
- 影響範囲: `package.json`, `scripts/next-dev-filter-onnx-clean-unused-initializer-lines-on-stderr.sh`, `docs/仕様書.md`, `docs/ローカル字幕翻訳_NLLB.md`, `.env.example`, `docs/変更履歴.md`。

- 変更内容: `instrumentation.ts` の `register` が `NEXT_RUNTIME !== "nodejs"` で早期 return しており、Node の通常起動（`NEXT_RUNTIME` 未設定）では **stderr フィルタも `ORT_LOG_*` 設定も一切実行されていなかった**。`NEXT_RUNTIME === "edge"` のときだけ return するよう修正。あわせて同じ ONNX 行が stdout に出る場合に備え `process.stdout.write` もラップ。
- 変更理由: ユーザー環境でフィルタが効かないとの報告の原因がこれだったため。
- 影響範囲: `src/instrumentation.ts`, `src/lib/translation/installOnnxRuntimeCleanUnusedInitializerWarningStderrFilterInDevelopmentIfEnabled.ts`, `docs/仕様書.md`, `docs/ローカル字幕翻訳_NLLB.md`, `docs/変更履歴.md`。

- 変更内容: macOS では `SessionOptions.logSeverityLevel` でも `graph.cc` の `CleanUnusedInitializers` 行が stderr に残るため、`NODE_ENV=development` のとき `process.stderr.write` をラップして該当チャンクだけ破棄する `installOnnxRuntimeCleanUnusedInitializerWarningStderrFilterInDevelopmentIfEnabled.ts` を追加。`instrumentation.ts` の `register` を async にし、Edge バンドル警告を避けるため **動的 import** で読み込む。無効化は `DISABLE_ONNX_CLEAN_UNUSED_INITIALIZER_STDERR_FILTER=1`。
- 変更理由: 前項の `InferenceSession.create` パッチだけでは実機で警告が消えなかったため。
- 影響範囲: `src/instrumentation.ts`, `src/lib/translation/installOnnxRuntimeCleanUnusedInitializerWarningStderrFilterInDevelopmentIfEnabled.ts`, `.env.example`, `docs/仕様書.md`, `docs/ローカル字幕翻訳_NLLB.md`, `docs/変更履歴.md`。

- 変更内容: ONNX の `CleanUnusedInitializers` 警告が macOS の `next dev` で `ORT_LOG_*` だけでは消えない場合に備え、`onnxruntime-node` の `InferenceSession.create` をラップして `SessionOptions.logSeverityLevel`（ERROR=3）をマージする `patchOnnxInferenceSessionCreateToMergeErrorOnlyLogSeverityLevel.ts` を追加。`translateEnglishTextListToJapaneseWithLocalNllbPipeline.ts` で transformers 直後に適用し、`env.backends.onnx.logLevel = "error"` も設定。
- 変更理由: `@xenova/transformers` がセッション作成時にログ閾値を渡さないため、ネイティブ側の WARNING が NSLog 経由で大量出力され続けるため。
- 影響範囲: `src/lib/translation/*`, `docs/仕様書.md`, `docs/ローカル字幕翻訳_NLLB.md`, `docs/変更履歴.md`。

- 変更内容: 字幕が出ない原因調査用に、`DEBUG_YOUTUBE_CAPTION_PIPELINE` / `NEXT_PUBLIC_DEBUG_YOUTUBE_CAPTION_PIPELINE` で有効化するデバッグログを追加。サーバーは `/api/captions` の取得件数・マージ後欠損・ローカル NLLB 前後、クライアントは fetch の HTTP 状態と `bilingualCueList` 件数を出力。`src/lib/debug/logYouTubeCaptionPipelineDebugIfEnabled.ts` 等。`.env.example` と仕様書の環境変数表を更新。
- 変更理由: ONNX ログと別に、データがどこで空になるかを切り分けるため。
- 影響範囲: `src/app/api/captions/route.ts`, `fillMissingJapaneseInBilingualTranscriptCueListWithMachineTranslation.ts`, `YouTubeLearningWatchPageContent.tsx`, `src/lib/debug/*`, `.env.example`, `docs/仕様書.md`, `docs/変更履歴.md`。

- 変更内容: ONNX の `CleanUnusedInitializers` 警告が `next dev` で残る問題に対し、`applyOnnxRuntimeLogSeverityDefaultsBeforeTransformersNativeBinding.ts` を `@xenova/transformers` より先に import。あわせて `package.json` の `dev` / `build` / `start` / `dev:turbo` に `ORT_LOG_SEVERITY_LEVEL=3` と `ORT_LOG_LEVEL=3` を前置。`instrumentation.ts` にも `ORT_LOG_LEVEL` を追加。
- 変更理由: `instrumentation` の `register()` だけではバンドル評価順により onnxruntime-node のロードより遅れることがあるため。
- 影響範囲: `src/lib/translation/*`, `src/instrumentation.ts`, `package.json`, `docs/仕様書.md`, `docs/ローカル字幕翻訳_NLLB.md`, `docs/変更履歴.md`。

- 変更内容: ローカル NLLB 利用時のターミナルノイズ対策。`src/instrumentation.ts` で Node 起動時に未設定なら `ORT_LOG_SEVERITY_LEVEL=3`（ONNX の WARNING 抑制）。`package.json` に直接依存 `sharp@0.34.5` と **`overrides` で `sharp` を全ツリー 0.34.5 に統一**し、`objc` / libvips 二重ロード警告を避ける。`translateEnglishTextListToJapaneseWithLocalNllbPipeline.ts` の `pipeline` 呼び出し型を実行用に絞ったキャストで `next build` の型エラーを解消。
- 変更理由: 開発時ログが読めない・macOS でネイティブ衝突の懸念があるため。
- 影響範囲: `package.json`, `package-lock.json`, `src/instrumentation.ts`, `src/lib/translation/translateEnglishTextListToJapaneseWithLocalNllbPipeline.ts`, `docs/ローカル字幕翻訳_NLLB.md`, `docs/変更履歴.md`。

- 変更内容: 欠損日本語の補完を **Google Cloud Translation / LibreTranslate から廃止**し、`@xenova/transformers`（**NLLB**・`Xenova/nllb-200-distilled-600M`）による**ローカル推論**に一本化。環境変数 `DISABLE_LOCAL_NLLB_SUBTITLE_TRANSLATION` で無効化可能。`next.config.ts` に `serverExternalPackages` を追加。`.cache/` を `.gitignore`。関連ドキュメントを [ローカル字幕翻訳_NLLB.md](./ローカル字幕翻訳_NLLB.md) に差し替え（旧 `Google_Cloud_Translation_API_設定.md` は削除）。API の `japaneseMachineTranslationProvider` は `"local_nllb"` のみ。
- 変更理由: 翻訳用の外部 API キー・課金 HTTP に依存せずローカルで完結させるため。
- 影響範囲: `package.json`, `next.config.ts`, `.gitignore`, `src/lib/translation/*`, `src/app/api/captions/route.ts`, `YouTubeLearningWatchPageContent.tsx`, `.env.example`, `docs/*`, `README.md`。

## 2026-04-06

- 変更内容: Google Cloud Translation フォールバックを強化。環境変数 `GOOGLE_TRANSLATION_API_KEY` を `GOOGLE_CLOUD_TRANSLATION_API_KEY` の別名として受理。英語トラックの言語コードを `source` に渡す。翻訳失敗時にサーバーログ出力。API に `japaneseMachineTranslationProvider` を追加。`docs/Google_Cloud_Translation_API_設定.md` を新設。
- 変更理由: 日本語字幕が無い動画を Translation API で確実に補い、設定手順を文書化するため。
- 影響範囲: `fillMissingJapaneseInBilingualTranscriptCueListWithMachineTranslation.ts`, `translateEnglishTextListToJapaneseWithGoogleCloudTranslationV2.ts`, `captions/route.ts`, `YouTubeLearningWatchPageContent.tsx`, `docs/*`, `.env.example`。

- 変更内容: `docs/仕様書.md` と `docs/設計_MVP_v1.md` を現行コードベースに合わせて全面改訂。若手向けに「読み方」、機能一覧、全 API の入出力、ディレクトリ別ファイル一覧、環境変数表、エラー表、字幕パイプラインのフロー（mermaid）を追加。
- 変更理由: 引き継ぎ・オンボーディングのしやすさのため。
- 影響範囲: `docs/仕様書.md`, `docs/設計_MVP_v1.md`, `README.md`（ドキュメントリンク整理）。

- 変更内容: オンラインデプロイ用に `docs/デプロイ手順.md` を追加し、`README.md` を本プロジェクト向けに更新。`.gitignore` で `.env.example` を追跡対象にした（`!.env.example`）。
- 変更理由: 本番公開手順と環境変数の明示。
- 影響範囲: `docs/デプロイ手順.md`, `README.md`, `.gitignore`, `docs/仕様書.md`。

- 変更内容: トップ検索のデフォルトクエリを「TED」に変更。
- 変更理由: 利用者の希望。
- 影響範囲: `YouTubeLearningSearchPageContent.tsx`, `docs/仕様書.md`。

- 変更内容: YouTube から日本語が欠ける行を、環境変数に基づき機械翻訳で補完。`GOOGLE_CLOUD_TRANSLATION_API_KEY`（Translation API v2・バッチ）を優先、なければ `LIBRETRANSLATE_BASE_URL`（並列・ローカル Docker 向け）。API 応答に `japaneseIsMachineTranslationFallback` を追加。`.env.example` をプレースホルダに整理（実キーの記載を避ける）。
- 変更理由: `tlang` 等で翻訳字幕が取れない環境でも、ローカルまたは Google で即時に日本語を表示するため。
- 影響範囲: `src/app/api/captions/route.ts`, `src/lib/translation/*`, `YouTubeLearningWatchPageContent.tsx`, `.env.example`, `docs/仕様書.md`。

- 変更内容: 英日字幕の時間軸マージを改善。区間が「すれ違いのみ」で重なり 0 になるケースに仮想スコアを付与し、走査打ち切りを「英語終了＋余裕秒」以降の日本語のみに限定。それでも付かない行は中点距離が 8 秒以内の日本語行をフォールバックで対応付け（YouTube 自動翻訳トラックの微ズレ・境界ずれ対策）。
- 変更理由: 日本語トラックは取れているのに UI が「日本語なし・時間軸で未対応」ばかりになる事象の修正。
- 影響範囲: `src/lib/captions/buildBilingualTranscriptCueList.ts`, `docs/仕様書.md`。

- 変更内容: LLM による字幕行補完（`POST /api/llm/subtitle-completion`、補完ボタン、関連キャッシュ・lib）を削除。日本語は `/api/captions` の YouTube 自動翻訳（`tlang`）等に一本化。単語解析の `word-analysis` のみ LLM を使用。
- 変更理由: 翻訳はボタン操作なしで自動化し、LLM は単語解析にのみ使うため。
- 影響範囲: `YouTubeLearningWatchPageContent.tsx`, `TranscriptScriptPane.tsx`, 削除 `src/app/api/llm/subtitle-completion/route.ts`, `generateSubtitleCompletionWithLlm.ts`, `translationCompletionCache.ts`, `youtubeEnglishLearningViewer.ts`（`SubtitleCompletionResult`）, `docs/*`。

- 変更内容: ネイティブ日本語字幕が無い動画で、英語トラックの baseUrl に `tlang=ja` を付けた YouTube 側の自動翻訳字幕を取得する。InnerTube・視聴ページ応答の両経路で試行し、ネイティブ日本語を自動翻訳より優先してマージ。英語のみ timedtext で取れた場合も同パラメータで再試行。API に `japaneseIsYoutubeAutoTranslation` を追加し、視聴ページに注記を表示。
- 変更理由: 日本語トラック未提供の動画でも学習用に日本語を表示するため（Google 翻訳 API は使わず YouTube が返す翻訳に依存）。
- 影響範囲: `src/app/api/captions/route.ts`, `fetchYouTubeCaptionXmlViaInnerTubePlayerApi.ts`, `fetchYouTubeCaptionXmlViaWatchPagePlayerResponse.ts`, `fetchYouTubeTimedTextXmlForLanguage.ts`, `YouTubeLearningWatchPageContent.tsx`, `docs/仕様書.md`。

- 変更内容: 検索ページのデフォルト検索クエリを「The Diary Of A CEO」に変更。
- 変更理由: 初回表示で当該チャンネル／番組向けの結果を出すため。
- 影響範囲: `src/components/search/YouTubeLearningSearchPageContent.tsx`。

- 変更内容: 初版実装（MVP v1）を追加。検索・視聴・字幕・スクリプト・単語解析・字幕補完 API と UI を作成。
- 変更理由: 要件定義に基づくプロダクト立ち上げ。
- 影響範囲: リポジトリ全体（新規作成）。

- 変更内容: 検索結果の `videoId` 重複（ページ追加時など）による React の key 警告を解消。結合時に既存 ID を除外し、1 ページ内の重複も除去。グリッドの key に一覧インデックスを併用。
- 変更理由: 「Encountered two children with the same key」対策。
- 影響範囲: `src/components/search/YouTubeLearningSearchPageContent.tsx`, `VideoSearchResultGrid.tsx`。

- 変更内容: 字幕取得を `ytInitialPlayerResponse` 経由（字幕トラックの `baseUrl`）を優先する方式に変更。従来の `/api/timedtext` 直叩きはフォールバック。404 時は診断メッセージを返す。
- 変更理由: サーバーからの素の timedtext リクエストが拒否され、全動画で字幕取得に失敗していたため。
- 影響範囲: `src/app/api/captions/route.ts`, `src/lib/captions/fetchYouTubeCaptionXmlViaWatchPagePlayerResponse.ts`, `src/lib/captions/extractBalancedJsonObjectSubstring.ts`。

- 変更内容: `ytInitialPlayerResponse` が HTML 内に複数ある場合、**字幕トラック数が最大のオブジェクト**を採用するよう変更。字幕 `baseUrl` の絶対化と **`fmt=srv3` の強制**（json3 既定の上書き）を追加。
- 変更理由: 先頭のプレイヤー応答に `captionTracks` が無く、特定動画で常に 404 になるケースへの対処。
- 影響範囲: `src/lib/captions/fetchYouTubeCaptionXmlViaWatchPagePlayerResponse.ts`。

- 変更内容: 字幕取得に **InnerTube `player`（ANDROID）＋ Android User-Agent での timedtext 取得**を最優先で追加。Web 由来の署名付き `baseUrl` がサーバーから空レスポンスになる問題に対処。XML の `<p t/d>`（format 3）形式をパース可能にした。
- 変更理由: `GET /api/captions` がトラックはあるのに本文 0 バイトで 404 になる事象の修正。
- 影響範囲: `src/lib/captions/fetchYouTubeCaptionXmlViaInnerTubePlayerApi.ts`（新規）, `parseYouTubeTimedTextXmlString.ts`, `fetchYouTubeCaptionXmlViaWatchPagePlayerResponse.ts`, `src/app/api/captions/route.ts`, `docs/仕様書.md`。

- 変更内容: `experimental.turbopackFileSystemCacheForDev: false` を設定し、Turbopack の SST 永続化エラー（`Unable to write SST file` 等）を回避。
- 変更理由: 開発環境でキャッシュ書き込みが失敗するケースへの対処。
- 影響範囲: `next.config.ts`。

- 変更内容: `npm run dev` を `next dev --webpack` に変更し、`dev:turbo` で Turbopack を明示選択可能にした。
- 変更理由: Turbopack 開発時の `ChunkLoadError`（古いチャンク参照）を避け、安定して開発できるようにするため。
- 影響範囲: `package.json`。

- 変更内容: 視聴ページをビューポート高（`h-dvh`）＋ `overflow-hidden` で固定し、グリッド行を `minmax(0,1fr)` で子の高さを親に合わせる。全文スクリプトは右カラム内のスクロール領域のみ縦スクロール。再生同期の行追従は `scrollIntoView` をやめ、スクリプト用スクロール要素の `scrollTo` のみで行う。単語解析パネルは最大高＋内部スクロール。
- 変更理由: 右ペインの字幕一覧が長いとページ全体が伸び、`scrollIntoView` でウィンドウまでスクロールしてしまうため。
- 影響範囲: `YouTubeLearningWatchPageContent.tsx`, `TranscriptScriptPane.tsx`, `docs/仕様書.md`。
